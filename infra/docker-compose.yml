services:
  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.0
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    networks:
      - autoresolve_net

  kafka:
    image: confluentinc/cp-kafka:7.5.0
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
    healthcheck:
      test: ["CMD", "kafka-topics", "--bootstrap-server", "localhost:9092", "--list"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - autoresolve_net

  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: autoresolve
    ports:
      - "5432:5432"
    networks:
      - autoresolve_net

  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
    networks:
      - autoresolve_net

  api-gateway:
    build: 
      context: ../
      dockerfile: Dockerfile
    command: ["uvicorn", "src.api.main:app", "--host", "0.0.0.0", "--port", "8000"]
    ports:
      - "8000:8000"
    environment:
      - KAFKA_BOOTSTRAP_SERVERS=kafka:9092
      - OPENAI_API_KEY=dummy_key_to_satisfy_pydantic
      - ANTHROPIC_API_KEY=dummy_key_to_satisfy_pydantic
      - QDRANT_URL=http://qdrant:6333
      - POSTGRES_DSN=postgresql://postgres:postgres@postgres:5432/autoresolve
    depends_on:
      kafka:
        condition: service_healthy
    networks:
      - autoresolve_net

  ai-worker:
    build:
      context: ../
      dockerfile: Dockerfile
    command: ["python", "-m", "src.agents.worker"]
    environment:
      - KAFKA_BOOTSTRAP_SERVERS=kafka:9092
      - OPENAI_API_KEY=dummy_key_to_satisfy_pydantic
      - ANTHROPIC_API_KEY=dummy_key_to_satisfy_pydantic
      - QDRANT_URL=http://qdrant:6333
      - POSTGRES_DSN=postgresql://postgres:postgres@postgres:5432/autoresolve
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/autoresolve
      - GITHUB_TOKEN=${GITHUB_TOKEN}
    depends_on:
      kafka:
        condition: service_healthy
      postgres:
        condition: service_started
      qdrant:
        condition: service_started
    networks:
      - autoresolve_net

networks:
  autoresolve_net:
    driver: bridge