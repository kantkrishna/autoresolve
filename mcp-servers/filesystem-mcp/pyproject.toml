[tool.poetry]
name = "filesystem-mcp"
version = "0.1.0"
description = "AutoResolve Filesystem Integration"
authors = ["AutoResolve <team@example.com>"]

[tool.poetry.dependencies]
python = "^3.12"
mcp = "^1.1.2"
